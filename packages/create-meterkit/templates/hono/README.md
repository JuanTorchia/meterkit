# MeterKit Hono API

Run `pnpm install && cp .env.example .env && pnpm dev`, then `pnpm check:unpaid` to inspect the HTTP 402 challenge. Use test assets on devnet for the paid retry and repeat it to prove replay rejection. Never supply a private key or seed phrase.
