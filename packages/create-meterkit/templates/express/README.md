# MeterKit Express API

Run `pnpm install && cp .env.example .env && pnpm dev`, then `pnpm check:unpaid` to inspect the HTTP 402 challenge. Fund only a disposable devnet wallet with test assets before following the paid retry. Repeat the same proof to confirm replay is rejected. Never place a private key or seed phrase in this project.
