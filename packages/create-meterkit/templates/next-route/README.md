# MeterKit Next route

Run `pnpm install && cp .env.example .env.local && pnpm dev`, then `pnpm check:unpaid` to inspect the HTTP 402 challenge. Use test assets on devnet for the paid retry and repeat it to verify replay rejection. Never store a private key or seed phrase here.
