# `@usemeterkit/cli`

Public MeterKit CLI for checking and diagnosing provider endpoints and for
performing bounded disposable Solana devnet verification flows.

The package is under active development. It is not yet the documented public
quickstart until the complete payment and replay lifecycle passes the release
gates in `specs/004-self-service-activation/`.
